spring:
  application:
    name: traveler-notification
  config:
    import: optional:configserver:http://localhost:8888
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:kavindadarmasiri15@gmail.com}
    password: ${EMAIL_PASSWORD:vhwv qtkl hfda pfzy}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

whatsapp:
  api:
    url: https://graph.facebook.com/v18.0
  phone:
    number:
      id: ${WHATSAPP_PHONE_NUMBER_ID:YOUR_PHONE_NUMBER_ID}
  access:
    token: ${WHATSAPP_ACCESS_TOKEN:YOUR_ACCESS_TOKEN}
