server:
  port: 8086

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/traveler_notification?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 20000
      idle-timeout: 300000
      max-lifetime: 1200000
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${EMAIL_USERNAME:kavindadarmasiri15@gmail.com}
    password: ${EMAIL_PASSWORD:vhwv qtkl hfda pfzy}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  main:
    allow-circular-references: true

whatsapp:
  api:
    url: https://graph.facebook.com/v18.0
  phone:
    number:
      id: ${WHATSAPP_PHONE_NUMBER_ID:YOUR_PHONE_NUMBER_ID}
  access:
    token: ${WHATSAPP_ACCESS_TOKEN:YOUR_ACCESS_TOKEN}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
