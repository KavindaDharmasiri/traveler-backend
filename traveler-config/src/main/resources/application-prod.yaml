spring:
  application:
    name: traveler-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/prod/
        git:
          uri: file:///tmp/config-repo
          default-label: main

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info