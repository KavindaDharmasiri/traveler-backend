spring:
  application:
    name: traveler-config
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config/
  profiles:
    active: native

server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"
